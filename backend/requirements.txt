# ── Cloud-Native Web Framework ──────────────────────────────────
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9       # Required for file uploads

# ── PDF Reading ────────────────────────────────────────────────
pymupdf==1.24.0               # PyMuPDF — fastest PDF text extractor

# ── Vector Database & AI ──────────────────────────────────────────
numpy<2.0.0                   # CRITICAL: Lock to 1.x to avoid NumPy 2.0 breaking changes
chromadb==0.4.24              # Updated for better stability
sentence-transformers==2.3.1  # all-MiniLM-L6-v2 for local embeddings
pysqlite3-binary              # Required for Linux (Hugging Face) stability

# ── LLM ───────────────────────────────────────────────────────
groq==0.4.2                   # Groq API client (free Llama-3 70B)

# ── Utilities ──────────────────────────────────────────────────
python-dotenv==1.0.0          # Load .env variables
pydantic==2.5.3               # Data validation (used by FastAPI)
requests==2.31.0              # HTTP requests for NewsAPI

# ── Phase 3: Agents & Reports ──────────────────────────────────
langgraph==0.0.26             # Multi-agent orchestration
fpdf2==2.7.8                  # Simple PDF generation
sse-starlette==2.0.0          # SSE for progress tracking